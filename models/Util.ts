// import { model, Schema } from "mongoose";

export interface IUtilSchema {
  name: string;
  commitData: string;
}

// const UtilSchema = new Schema<IUtilSchema>({
//   name: { type: String, required: true, unique: true },
//   commitData: { type: String, required: true },
// });

// export const Util = model("Util", UtilSchema);
